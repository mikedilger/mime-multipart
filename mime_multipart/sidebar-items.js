initSidebarItems({"enum":[["Node","A multipart part which could be either a file, in memory, or another multipart container containing nested parts."]],"fn":[["get_multipart_boundary","Get the `multipart/*` boundary string from `hyper::Headers`"],["parse_multipart","Parse a MIME multipart/* into a `Vec` of `Node`s.  You must pass in the MIME boundary string, which can be retrieved by calling get_multipart_boundary() on the `Headers`.  If `always_use_files` is true, all parts will be streamed to files.  If false, only parts with a `Filename` `ContentDisposition` header will be streamed to files.  Recursive `multipart/*` parts will are parsed as well and returned as `Node::Multipart` variant."]],"mod":[["error",""],["filepart",""]],"struct":[["Part","A multipart part which is not a file (stored in memory)"]]});